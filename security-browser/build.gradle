dependencies {
    compile project(':security-core')
    // 该项目用来做浏览器端的所以需要有session
    // 提供集群环境下的session管理,也没有被管理到，需要自己添加
    //如果在启动的时候报错，可以先注释掉，没有注意视频中是怎么通过配置把这个关闭的了
//    compile('org.springframework.session:spring-session:1.3.3.RELEASE')
}